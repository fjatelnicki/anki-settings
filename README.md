Usage:
```
cd "/Users/$USER/Library/Application Support/Anki2/addons21"
```

```
git clone --depth 1 https://github.com/fjatelnicki/anki-settings.git temp-dir && mv temp-dir/* temp-dir/.* . && rmdir temp-dir
```
